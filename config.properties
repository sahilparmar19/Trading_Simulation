db.url=jdbc:postgresql://localhost:5432/tradingdb
db.user=postgres
db.password=sahil

# Alpha Vantage API key for live price fetching.
# Get a free key at: https://www.alphavantage.co/support/#api-key
# The free tier allows 25 requests/day and 5 requests/minute.
# If left as "demo", only the AAPL demo quote is available;
# all other tickers will fall back to the existing random price logic.
alphavantage.apikey=demo
